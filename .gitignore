node_modules
dist
dist-server
.env
logs/

# Vitest coverage — báo cáo sinh tự động (text + html), không commit
coverage/

# Playwright E2E — báo cáo & kết quả sinh tự động, không commit
playwright-report/
test-results/
blob-report/
playwright/.cache/

# File audio cache khi STORAGE_DRIVER=local (xem api/_lib/fileStorage.ts) — tự tạo lại
# được từ Google TTS, không cần commit lên Git.
uploads/
scripts/prefetch-tts-errors.json
.env.local
*.local

# Tự tạo khi chạy "npm run seed:pronunciation" — danh sách từ bị lỗi để retry, không cần commit
scripts/seed-errors.json

# Thư mục clone trùng lặp, không phải 1 phần của project (xem báo cáo build)
/bilingual-english-vietnamese/

# Build/verify tạm thời & cache — không commit
dist_verify_tmp*/
vite.config.ts.timestamp-*.mjs
tsconfig.node.tsbuildinfo

# Sinh tự động lúc build bởi scripts/gen-data-manifest.mjs (chạy trước vite build).
# Mỗi lần build sẽ tạo lại (đổi generatedAt) → đừng theo dõi để git status không "bẩn".
apps/dhcb/public/data/manifest.json

# Bản sao lưu từ điển tạm khi mở rộng lên 10.000 từ
apps/dhcb/src/data/dictionary.backup.json
.claude/*
!.claude/settings.json
!.claude/report-status.sh
!.claude/agents
!.claude/agents/*.md
!.claude/hooks
!.claude/hooks/*.sh
!.claude/commands
!.claude/commands/*.md
!.claude/usage-budget.example.sh

# File cũ đã xóa khỏi project, giữ tạm trên đĩa do hạn chế của sandbox — không commit
.trash/

dist_verify_tmp*/
.trash/

# Tài liệu SGK có bản quyền (PDF do người dùng cấp để AI đọc tra cứu phạm vi/thứ tự bài).
# TUYỆT ĐỐI KHÔNG commit lên GitHub — chỉ đọc tại chỗ, nội dung app phải tự soạn mới.
tai-lieu-sgk/

# Bản đồ code sinh tự động bởi `npm run codemap` — không commit (dựng lại được)
.codemap/

# Output build của từng gói workspace (tsc -b, phương án B project references)
packages/*/dist
*.tsbuildinfo
