# postgres/migrations

Thư mục lưu thay đổi schema PostgreSQL tự host theo thời gian (incremental), giống
cơ chế `supabase/migrations/` cũ nhưng cho DB tự host trên VPS.

- DB mới (lần đầu): `npm run migrate:pg` tự áp `../schema.sql` (đầy đủ, idempotent)
  RỒI mới áp các file lẻ trong thư mục này.
- Đặt tên file `NNNN_mo-ta-ngan.sql` (số thứ tự 4 chữ số + mô tả), viết idempotent
  (`create or replace`, `if not exists`).
- **Tự động áp khi deploy** — `scripts/deploy.sh` gọi `npm run migrate:pg` ở MỌI lượt deploy,
  và deploy tự chạy khi push/merge lên `main` (`.github/workflows/deploy.yml`, trigger
  `push: branches: [main]`). Khác cơ chế Supabase cũ (phải tự tay chạy migration production) —
  ở đây **không cần thao tác tay**, chỉ cần merge xong PR đổi schema là migration đã áp.
  Muốn xác nhận thủ công: SSH VPS, xem log deploy gần nhất hoặc tự chạy lại `npm run migrate:pg`
  (idempotent, chạy lại không sao).

## Danh sách migration đã có

| File                                   | Nội dung                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0001_app_settings.sql`                | Bảng `app_settings` (hạn mức/khuyến mãi hiện hành, đọc công khai qua `/api/app-settings`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0002_age_group.sql`                   | Cột `profiles.age_group` (nhóm tuổi tự chọn, giao diện/nội dung theo độ tuổi).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0003_pronunciation_lang_key.sql`      | Đổi khoá duy nhất bảng `pronunciations` từ `(word, voice)` thành `(word, voice, lang)` — tránh 1 chữ trùng giữa tiếng Anh/Việt đè cache lẫn nhau.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0004_plan_expires_at.sql`             | Cột `profiles.plan_expires_at` — hạn dùng gói Pro/VIP tự hết hạn, cấp gói thủ công qua admin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0005_ai_circuit_breaker.sql`          | Cột `app_settings.ai_circuit_breaker` — cầu dao khẩn cấp chặn toàn bộ lượt gọi AI (GĐ3 kế hoạch scale 50k, xem `docs/research/ke-hoach-scale-30k-concurrent.md`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0006_analytics_events.sql`            | Bảng `analytics_events` — analytics tự viết (không dùng script bên thứ 3) đo hiệu quả kênh marketing, ghi qua `/api/analytics`, auth tuỳ chọn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0007_referral.sql`                    | Cột `profiles.referral_code` + bảng `referrals` — mời bạn, thưởng ngày gói Pro cho cả 2 bên khi người được mời học thật (xem `api/_lib/referral.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0008_referral_device.sql`             | Cột `referrals.device_hash` — dấu vân tay thiết bị (best-effort) chỉ dùng để TỪ CHỐI THƯỞNG khi cày nhiều tài khoản trên cùng máy, không khoá tài khoản.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0009_email_verification.sql`          | Bảng `email_verifications` — mã 6 chữ số xác thực email, chống email giả cày thưởng mời bạn (cột `users.email_verified` đã có sẵn từ schema gốc).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0010_email_daily_usage.sql`           | Bảng `email_daily_usage` — đếm thư đã gửi mỗi ngày theo kênh, để tự chuyển sang kênh dự phòng (Amazon SES) khi kênh chính chạm trần hạn mức.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0011_password_reset.sql`              | Bảng `password_resets` — quên mật khẩu, gửi link reset qua email (token dài, khác mã 6 chữ số của xác thực email vì đây là đường chiếm quyền tài khoản).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0012_free_weekly_ai_credit.sql`       | Bảng `weekly_ai_credit` + hàm `grant_weekly_ai_bonus`/`consume_weekly_ai_credit`/`refund_weekly_ai_credit` — kho lượt AI CHUNG (mọi tính năng) tích luỹ theo tuần cho gói Free, +5/ngày học thật, tối đa 35/tuần, reset thứ Hai. Chỉ áp cho Free — Pro/VIP vẫn dùng `daily_usage` như cũ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0013_email_verify_trial.sql`          | Cột `profiles.trial_granted_at` — tặng 5 ngày Pro khi xác thực email lần đầu. **Đã ngừng dùng** từ `0019_signup_trial.sql` (thay bằng quà 14 ngày), cột giữ lại chỉ để lưu dữ liệu lịch sử.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0014_plan_prices.sql`                 | Bảng `plan_prices` — giá bán Pro/VIP theo 3 chu kỳ (`10day`/`month`/`year`), có `sale_price_vnd`/`sale_until` cho khuyến mãi dịp lễ, đọc qua `api/_lib/prices.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0015_payments.sql`                    | Bảng `payments` — đơn thanh toán qua SePay, khớp bằng `payment_code` tự sinh in trong nội dung chuyển khoản, chống trùng webhook bằng UNIQUE `provider_txn_id` (`api/payment-webhook.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0016_daily_total_limit.sql`           | Hạn mức Pro/VIP đổi từ 5 cột riêng theo chế độ sang MỘT cột tổng lượt/ngày mỗi gói (`pro_daily_limit`/`vip_daily_limit`) + hàm `consume_usage_total` (SUM cả 5 cột daily_usage so với hạn mức). Xoá 15 cột cũ (5 free chết + 5 pro + 5 vip theo chế độ) — free vốn đã enforce qua weekly_ai_credit (0012), không phải cột này.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0017_free_rolling_credit.sql`         | Kho lượt AI gói Free đổi từ "tuần lịch" (reset thứ Hai, bảng `weekly_ai_credit` của 0012) sang CỬA SỔ TRƯỢT 7 ngày liền kề thật — bảng `free_daily_credit` (1 dòng/ngày/user) + hàm `grant_daily_bonus_rolling`/`consume_rolling_credit`/`refund_rolling_credit`. `weekly_ai_credit` (0012) giữ nguyên, không xoá, chỉ ngừng đọc/ghi.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0018_leaderboard_toggle.sql`          | Cột `app_settings.leaderboard_enabled` — cầu dao tạm tắt Bảng xếp hạng (`/challenge`) khi ít người dùng khiến bảng trông vắng vẻ, admin tự bật lại qua `/admin-settings` khi đủ đông, không cần deploy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0019_signup_trial.sql`                | Cột `profiles.signup_trial_granted_at` — tặng 14 ngày Pro cho tài khoản MỚI đã xác thực (email/password: lúc xác thực mã; Google: ngay lần đăng nhập đầu vì đã tự xác minh). Thay hẳn cơ chế 5 ngày cũ (0013), không cộng dồn (`api/_lib/trial.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0020_facebook_apple_login.sql`        | Cột `users.facebook_id`/`apple_id` (cùng khuôn mẫu `google_id`) — đăng nhập Facebook/Apple, 1 user liên kết được cả 3 kênh qua email trùng (`api/_lib/authService.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0021_quest_claims.sql`                | Bảng `quest_claims` — hạ tầng nhiệm vụ (quest) dùng chung theo `quest_key` + cooldown, thêm nhiệm vụ mới không cần migration. Nhiệm vụ đầu tiên: "Chia sẻ công khai" (+1 ngày Pro, hồi sau 7 ngày, `api/_lib/quests.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0022_microsoft_login.sql`             | Cột `users.microsoft_id` — đăng nhập Microsoft, cùng khuôn mẫu `google_id`/`facebook_id`/`apple_id` (`api/_lib/authService.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0023_vip_whitelist.sql`               | Bảng `vip_whitelist` — danh sách email được cấp VIP vĩnh viễn thủ công, SỐNG (còn trong bảng = VIP, admin xoá khỏi bảng = hạ về free), quản lý qua `/admin` (`api/admin-vip-whitelist.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0024_plan_features.sql`               | Bảng `feature_catalog` + `plan_feature_flags` — ma trận bật/tắt từng tính năng theo từng gói (Free/Pro/VIP), quản lý qua `/admin` tab "Tính năng theo gói" (`api/admin-plan-features.ts`). Khác `app_settings` (số hạn mức lượt/ngày).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `0025_plan_marketing.sql`              | Bảng `plan_marketing_info` + `plan_marketing_bullets` — nội dung mô tả gói (badge/tagline + gạch đầu dòng) hiển thị ở trang Nâng cấp, quản lý qua `/admin` tab "Nội dung gói" (`api/admin-plan-marketing.ts`). Thuần nội dung quảng cáo, không ảnh hưởng quyền truy cập.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0026_achievement_rewards.sql`         | Bảng `achievement_rewards` — phần thưởng (ngày Pro/VIP) cho từng huy hiệu, admin bật/tắt + cấu hình số ngày, áp dụng ngay không cần deploy. Xác minh "đã đạt huy hiệu" làm ở SERVER, không tin danh sách client gửi lên (`api/_lib/achievementRewards.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0026_price_promo.sql`                 | Bảng `price_promo` (1 dòng) — khuyến mãi % áp dụng cho TOÀN BỘ gói/chu kỳ cùng lúc kèm ngày bắt đầu/kết thúc, quản lý qua `/admin` (`api/admin-price-promo.ts`). Khác `plan_prices.sale_price_vnd` (giá tuyệt đối riêng từng dòng). Trùng số thứ tự với `0026_achievement_rewards.sql` (2 PR độc lập cùng lấy số 0026) — vô hại vì `migrate:pg` theo dõi theo TÊN FILE, không theo số, nhưng phá quy ước đánh số tuần tự.                                                                                                                                                                                                                                                                                                                                                                           |
| `0027_payments_years.sql`              | Cột `payments.years` — cho phép mua nhiều năm liền 1 lần ở chu kỳ `year` (giảm giá luỹ tiến theo số năm), ghi lại số năm đã mua để `api/payment-webhook.ts` cấp đúng số ngày.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0027_reserved_names.sql`              | Bảng `reserved_names` — danh sách từ cấm / tên mạo danh ban quản trị (`admin`, `quan tri`…), chặn người dùng đặt biệt danh giả danh admin. Migration tự nạp sẵn danh sách mặc định.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0028_tts_viseme_timeline.sql`         | Cột `tts_cache.viseme_timeline` (jsonb) — lưu timeline khẩu hình (viseme) THẬT lấy từ ElevenLabs `/with-timestamps` kèm audio đã cache, thay việc client tự ước lượng bằng chia đều thời lượng. NULL = chưa có, client tự ước lượng như trước.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0029_platform_subject.sql`            | Cột `subject` thêm vào cả `free_daily_credit` (0017) và `daily_usage`/`consume_usage_total` (0016) — mỗi môn học đếm/trừ lượt riêng dùng chung 1 hạn mức, chuẩn bị nền tảng đa lĩnh vực (`docs/adr/0001-nen-tang-da-linh-vuc.md`). Backfill dữ liệu cũ về `subject='english'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0030_schema_english.sql`              | Chuyển 7 bảng dữ liệu học tiếng Anh sang schema Postgres riêng `english` (chuẩn bị môn thứ 2 có schema riêng), kèm view compat `public.<bảng>` trỏ sang `english.<bảng>` cho mã/bản build cũ. `tts_cache`/`daily_usage`/`free_daily_credit` ở lại `public` (hạ tầng dùng chung mọi môn).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0031_tts_cache_pending.sql`           | Bảng `tts_cache_pending` — khoá "claim" chống race condition khi nhiều request cùng cache-miss 1 câu TTS giống nhau (trước đó có thể tốn tiền API trùng lặp + tái sử dụng nonce AES-GCM), xem `packages/core-ai/tts.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0032_listening_feature.sql`           | Thêm khoá tính năng `listening` (trang `/listening`, thư viện Nghe) vào ma trận `feature_catalog`/`plan_feature_flags` (0024), mở bật cho cả 3 gói Free/Pro/VIP ngay từ đầu.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0033_email_reminders.sql`             | Bảng `email_reminders` (1 dòng/user, `last_sent_at`) — nhớ lần gửi email nhắc học gần nhất để KHÔNG gửi trùng/spam.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0034_identities.sql`                  | Bảng `identities(provider, provider_user_id, user_id, email, linked_at)` — thay 4 cột `google_id`/`facebook_id`/`apple_id`/`microsoft_id` trên `users` (Bước 1 kế hoạch đa lĩnh vực, `docs/adr/0002-quan-ly-nguoi-dung.md`). Backfill từ 4 cột cũ; `authService.ts` dual-write cả cột cũ lẫn bảng mới, chưa đổi chiều đọc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0035_entitlements.sql`                | Bảng `entitlements(user_id, product, tier, source, granted_at, expires_at)` — quyền lợi gói cước theo từng sản phẩm/môn (Bước 2 kế hoạch đa lĩnh vực, `docs/adr/0002-quan-ly-nguoi-dung.md`). CHỈ thêm bảng + backfill từ `profiles.plan`/`plan_expires_at` — billing/usage/admin vẫn đọc `profiles.plan` như cũ, chưa có code nào đọc bảng này.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `0036_english_user_profile.sql`        | Bảng `english.user_profile(user_id, user_level, goal, daily_minutes, age_group)` — tách dữ liệu onboarding CHỈ ĐÚNG VỚI tiếng Anh ra khỏi `public.profiles` (Bước 4 kế hoạch đa lĩnh vực, `docs/adr/0002-quan-ly-nguoi-dung.md`). CHỈ thêm bảng + backfill — `api/profile.ts` vẫn đọc/ghi 4 cột cũ trên `profiles` như cũ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0037_drop_oauth_columns.sql`          | Xoá `google_id`/`facebook_id`/`apple_id`/`microsoft_id` khỏi `public.users` (Bước 6 kế hoạch đa lĩnh vực, `docs/adr/0002-quan-ly-nguoi-dung.md`) — thay bằng tra cứu qua bảng `identities` (0034). PHẢI deploy CÙNG LÚC với thay đổi `packages/core-auth/authService.ts` (đổi chiều đọc trước khi xoá cột).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0038_tts_cache_iv.sql`                | Cột `tts_cache.iv` — lưu IV/nonce AES-256-GCM THẬT (base64) của từng bản ghi audio. Trước đây iv được SUY RA từ `hash` nên mọi lần mã hoá lại cùng một hash đều dùng lại đúng nonce — lỗi mật mã nghiêm trọng của AES-GCM (xem `api/_lib/ttsCrypto.ts`). Cột để NULL được: bản ghi cũ vẫn giải mã bằng iv suy từ hash, không cần sinh lại audio đã trả tiền.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `0039_tts_cache_stats.sql`             | Bảng `tts_cache_stats` (đếm hit/miss cache TTS theo ngày+lang+voice) và `tts_cache_audit` (kết quả quét đối chiếu DB ↔ Cloudflare R2, chạy nền). Phục vụ tab admin "Cache TTS & R2": tỉ lệ cache hit, số dòng trỏ sai chỗ, số file thiếu/orphan trên R2. Chỉ là số liệu thống kê — xoá đi không mất audio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0040_sync_user_settings.sql`          | 2 cột `english.learning_progress.settings` (jsonb: ngôn ngữ giao diện, chiều học Anh⇄Việt, âm thanh, giọng TTS) và `streak_freeze_dates` (mảng ngày) — trước đây CHỈ ở localStorage nên đổi máy là mất. `settings` hợp nhất theo `updatedAt` mới hơn thắng; `streak_freeze_dates` hợp nhất UNION (chỉ tăng, không mất vé nghỉ đã ghi ở máy khác).                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0041_personal_world_model.sql`        | Schema MỚI `personal` + 2 bảng `personal.persons` (1-1 với `public.users`) / `personal.personal_facts` — Personal World Model (V2-03, `docs/architecture-v2/21-ROADMAP.md`). Fact có provenance/confidence/sensitivity/expiry; sửa/xoá là APPEND — bản cũ chỉ hạ cờ `is_current`, KHÔNG bao giờ `delete` dòng, để giữ audit trail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0042_consent_and_policy.sql`          | 2 bảng `personal.consent_grants` / `personal.personal_policies` — Consent + Personal Policy (V2-04 slice 1, `docs/architecture-v2/21-ROADMAP.md`). Consent theo (scope, purpose) có version/expiry/revoke; policy gán mức thẩm quyền READ…AUTOMATE/DENY cho (subject, action, resource_scope). Đều APPEND-ONLY: thu hồi chỉ set `status`/`revoked_at`, cấp lại là dòng MỚI version tăng — không `delete`, không update giá trị nghiệp vụ. Partial unique index đảm bảo tối đa 1 bản đang hiệu lực mỗi bộ khoá.                                                                                                                                                                                                                                                                                      |
| `0043_life_graph.sql`                  | Life Graph foundation V2-05: node/edge/LifeGoal trong schema `personal`, composite FK chặn orphan và cross-user edge, soft archive + audit log append-only. `life_goal_sources` chỉ lưu định danh nguồn Learning để backfill/read-view idempotent; không sao chép payload `daily_minutes` và không thay nguồn thật `public.profiles`.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0044_personal_memory.sql`             | 2 bảng `personal.memory_records` + `memory_records_audit_log` — Personal Knowledge Fabric (V2-06): bộ nhớ cá nhân chia theo namespace (`semantic`/`episodic`/`preference`/`commitment`/`domain`), kèm provenance, mức nhạy cảm và chính sách lưu giữ. Rollback ưu tiên gỡ code và GIỮ bảng để không mất audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `0045_proposed_actions.sql`            | 2 bảng `personal.proposed_actions` + `tool_execution_audit_log` (V2-08) — mọi hành động AI đề xuất phải qua vòng `pending → confirmed/rejected → committed`, có `risk_level` và nhật ký thực thi công cụ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0046_decision_records.sql`            | 2 bảng `personal.decision_records` + `decision_reviews_audit_log` (V2-10) — Decision Ledger: ghi quyết định đã ra và vòng đối chiếu kết quả thực tế về sau.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0047_career_domain.sql`               | Schema MỚI `career` + `profiles`/`experiences`/`goals` (V2-13) — trụ Career, mảng ngoài-học-tập đầu tiên chứng minh khuôn Personal OS dùng được cho nhiều lĩnh vực.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `0048_work_domain.sql`                 | Schema MỚI `work` + `projects`/`tasks`/`meetings`/`documents` (V2-15) — trụ Work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0049_startup_domain.sql`              | Schema MỚI `startup` + `ventures`/`problems`/`hypotheses`/`evidence` (V2-16) — trụ Startup theo lối kiểm chứng giả thuyết.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `0050_life_foundation.sql`             | Schema MỚI `life` + `plans`/`habits`/`habit_logs`/`wellbeing_checks`/`growth_milestones` (V2-17) — trụ Life. Các mảng nhạy cảm (sức khoẻ tinh thần) có thêm lớp chính sách riêng.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0051_approved_automation.sql`         | 2 bảng `personal.automation_grants` + `action_receipts` (V2-18) — tự động hoá CHỈ chạy khi người dùng cấp quyền tường minh: có ngân sách/hạn mức, kích hoạt theo lịch hoặc sự kiện, thu hồi/tạm dừng được, và biên nhận hành động bất biến.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `0052_person_erasure_log.sql`          | Bảng `platform.person_erasure_log` (V2-19) — nhật ký APPEND-ONLY các yêu cầu xoá toàn bộ dữ liệu một người. Không bao giờ xoá dòng trong bảng này (chính nó là bằng chứng đã xoá).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0053_friends.sql`                     | Cột `profiles.friend_code` + bảng `friendships` — kết bạn qua mã/URL/QR cá nhân, KHÔNG có luồng gửi/chấp nhận lời mời (chia sẻ link đã là đồng ý, quét link xác nhận 1 lần là thành bạn 2 chiều). KHÁC `referral_code` (0007): referral mời người MỚI đăng ký và có thưởng, friend_code nối 2 người ĐÃ có tài khoản và không thưởng gì.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `0054_chat.sql`                        | Schema MỚI `chat` + `rooms`/`room_members`/`messages`/`moderation_events` — nhắn tin thời gian thực 1-1 giữa BẠN BÈ (xem 0053). Giai đoạn 1 chỉ DM; schema chừa sẵn cờ `is_group` nhưng chưa dùng.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0055_add_plus_plan_pricing.sql`       | Mở rộng hệ 3 gói thành 4: Free · **Plus** · Pro · VIP. Nới check constraint `plan_prices`/`plan_feature_flags` để nhận `'plus'` rồi nạp giá + cờ tính năng cho gói mới. Additive, tương thích ngược.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `0056_user_feedback.sql`               | Bảng `user_feedback` — tiếp nhận góp ý/báo lỗi từ người dùng, phân loại `bug`/`feature`/`content`/`ui_ux`/`other`. Nhận được cả từ khách CHƯA đăng nhập (`user_id` cho phép null).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `0057_feature_status_checks.sql`       | Bảng `feature_status_checks` — lịch sử kiểm tra sống/chết của các tính năng phụ thuộc bên thứ ba (AI chat, STT, TTS, R2, SePay) và Postgres, phục vụ mục "Trạng thái hệ thống" trong trang Admin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `0058_platform_feature_state.sql`      | Schema MỚI `platform` + bảng `feature_state` (mỗi cặp user×tính năng một dòng JSONB) — thay các `Map` in-memory cấp module vốn mất khi restart và VỠ trong PM2 cluster (mỗi tiến trình một bản copy). Xem `packages/core-db/featureState.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `0059_platform_vs_subject_profile.sql` | Chốt ranh giới dữ liệu NỀN TẢNG vs MÔN HỌC: đánh thức bảng `english.user_profile` mà `0036` tạo sẵn rồi để ngủ (`api/core/profile.ts` từ đây ghi cả hai nơi trong một transaction, đọc ưu tiên bảng môn rồi rơi về `public.profiles`). Sửa một phân loại sai của `0036`: `age_group` là dữ liệu NỀN TẢNG (quyết định giọng Companion, nội dung theo lứa tuổi, băng tuổi hồ sơ năng lực — dùng cho MỌI môn) nên xoá cột trùng bên bảng môn, giữ một nguồn sự thật duy nhất ở `public.profiles`.                                                                                                                                                                                                                                                                                                      |
| `0060_two_factor_auth.sql`             | Xác thực hai bước TOTP (RFC 6238): `user_2fa` (secret ĐÃ MÃ HOÁ bằng `core-config/userDataCrypto.ts`, `last_used_step` chặn dùng lại mã) + `user_2fa_recovery_codes` (băm SHA-256, dùng một lần) + cột `sessions.stepup_until` cho cửa sổ nâng quyền 15 phút. 2FA là TUỲ CHỌN — bắt buộc sẽ chặn học sinh 10–18 chưa có điện thoại riêng; nó chỉ gác thao tác nhạy cảm, học/chat bình thường không cần.                                                                                                                                                                                                                                                                                                                                                                                             |
| `0061_personal_intake.sql`             | Luồng NGƯỜI MỚI (5 câu ~90 giây → hồ sơ ẩn → gợi ý một việc): bảng `personal.intake`. Hai câu TỰ DO (câu 3, 4) mã hoá — chúng là lời người dùng tự nói về bản thân và câu 4 còn là tín hiệu năng khiếu; các câu chọn-sẵn để nguyên vì cần lọc/thống kê và chỉ có 6/4 khả năng nên giấu cũng vô nghĩa. KHÔNG giữ bản sao `age_group` — nguồn sự thật vẫn ở `public.profiles` (giữ đúng ranh giới `0059` vừa dọn).                                                                                                                                                                                                                                                                                                                                                                                    |
| `0062_intake_outcome.sql`              | ĐO xem gợi ý của luồng người mới có TRÚNG không: thêm `suggested_task_id`, `task_chosen_at`, `task_done_at`. Lưu thứ ĐÃ gợi ý thay vì tính lại — hàm sinh gợi ý là hàm thuần nên hôm nay tính lại vẫn đúng, nhưng sửa thuật toán ngày mai sẽ làm đổi toàn bộ số liệu lịch sử và mất khả năng so sánh trước/sau cải tiến.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `0063_english_mistakes.sql`            | SỔ TAY LỖI SAI lên server (`english.mistakes`) — trước đây sổ chỉ nằm `localStorage` nên đổi máy/xoá cache là mất sạch thứ tài liệu ôn giá trị nhất của từng người. Khoá tự nhiên `(user_id, dedupe_key)` dùng ĐÚNG khoá gộp lỗi trùng của client (`norm(wrong)→norm(corrected)`) nên đồng bộ hai chiều không sinh bản trùng. `created_at` là lần mắc lỗi GẦN NHẤT (không phải lúc tạo dòng) nên cố tình không đặt `default now()`.                                                                                                                                                                                                                                                                                                                                                                 |
| `0064_programming_schema.sql`          | Khung dữ liệu môn LẬP TRÌNH (PR-L1, đặc tả `dac-ta-mon-lap-trinh-2026-08-24.md` + `dac-ta-du-an-xuyen-suot-mon-lap-trinh-2026-08-24.md`): schema `programming` với `learner_state` (bậc P1–P6 + dự án trục T1/T2/T3), `lesson_progress` (khoá bài từ `subject-programming/curriculum.ts`), `project_files` (workspace dự án per-user, chặn 256KB/file ở DB, quota tổng ~2MB kiểm ở API) và `project_snapshots` (chốt theo milestone chặng để xem lại/khôi phục). Endpoint đọc/ghi vào ở PR-L3/L3b.                                                                                                                                                                                                                                                                                                  |
| `0065_code_feedback_usage.sql`         | Mode đếm lượt MỚI `code_feedback` cho môn Lập trình (PR-L5): thêm cột `daily_usage.code_feedback_count` và **sửa cả 3 hàm SQL** `consume_usage`/`refund_usage`/`consume_usage_total` — chúng whitelist tên cột bằng danh sách cứng và cộng TAY từng cột để ra tổng ngày, nên chỉ thêm cột mà quên hàm thì lượt AI của môn Lập trình vừa không đếm được (fail-open = miễn phí không giới hạn) vừa lọt khỏi hạn mức tổng của Pro/VIP.                                                                                                                                                                                                                                                                                                                                                                 |
| `0066_worklife_merge.sql`              | Gộp trụ WORK và LIFE thành MỘT trụ "Công việc & Đời sống" (quyết định người dùng 2026-08-25): tạo schema `worklife` rồi `alter table ... set schema` toàn bộ 9 bảng của `work` (projects/tasks/meetings/documents) và `life` (plans/habits/habit_logs/wellbeing_checks/growth_milestones), sau đó `drop schema ... restrict` hai schema cũ đã rỗng. **Không sao chép dữ liệu** — chỉ đổi chỗ đứng trong catalog, giữ nguyên hàng/khoá ngoại/chỉ mục; lũy đẳng (chạy lại không lỗi) và lùi được bằng lệnh SET SCHEMA ngược, ghi sẵn ở cuối file migration.                                                                                                                                                                                                                                           |
| `0067_companion_conversation.sql`      | Lưu hội thoại với Bạn Đồng Hành AI (`personal.companion_messages`). Trước đó KHÔNG bảng nào chứa lượt thoại Companion: đóng trang là mất sạch, và LLM chỉ nhận đúng MỘT tin nhắn mỗi lượt nên không nhớ gì. Bảng phục vụ hai việc: nạp lại hội thoại khi mở lại trang, và ghép vài lượt gần nhất vào prompt gửi LLM. Khác `personal.memory_records` (trí nhớ đã chọn lọc, phải qua xác nhận mới ghi) — bảng này là bản ghi thô của cuộc trò chuyện.                                                                                                                                                                                                                                                                                                                                                 |
| `0059_ai_token_usage_daily.sql`        | Bảng `platform.ai_token_usage_daily` — chi phí AI đo theo TOKEN THẬT do nhà cung cấp báo về, cộng dồn theo (ngày giờ VN, provider, model, chế độ). Không log từng lượt gọi và KHÔNG lưu `user_id` (số liệu vận hành gộp, không phải nhật ký hành vi cá nhân). Xem `packages/core-ai/aiTokenUsage.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `0068_location_sharing.sql`            | Schema `location` cho tính năng "Đi chung" (chia sẻ vị trí thời gian thực khi đi chơi cùng bạn bè): `sessions` (chuyến, mã mời, điểm hẹn, `expires_at` bắt buộc — không có chia sẻ vĩnh viễn), `session_members` (công tắc `sharing_enabled` + chế độ `precision_mode` exact/approx), `positions` (CHỈ vị trí mới nhất, 1 dòng/người, không lưu lịch sử hành trình) và `consent_log` (nhật ký bật/tắt/vào/rời, không chứa toạ độ).                                                                                                                                                                                                                                                                                                                                                                  |
| `0069_companion_links.sql`             | "Người thân theo dõi" (đặc tả `dac-ta-nguoi-dong-hanh-2026-08-26.md`): `companion_links` (liên kết MỘT CHIỀU learner → watcher, tối đa 2 người theo dõi mỗi người học, cột `last_report_at` dùng để chống gửi trùng báo cáo tuần) và `companion_invites` (mã mời **dùng một lần, hạn 24 giờ** — khác `profiles.friend_code` vốn vĩnh viễn, vì quyền xem tiến độ học nặng hơn quyền kết bạn). Người theo dõi chỉ nhận báo cáo TUẦN qua email; không có bảng/route nào cho họ đọc dữ liệu học theo thời gian thực.                                                                                                                                                                                                                                                                                    |
| `0070_exam_plans.sql`                  | Chế độ ôn thi có hạn chót (đặc tả `dac-ta-che-do-on-thi-2026-08-26.md`): bảng `exam_plans` (kỳ thi, ngày thi, phạm vi, trần mỗi ngày, ngày nghỉ trong tuần) + partial unique index bắt **một kế hoạch đang chạy mỗi người**. Cố ý KHÔNG có bảng lịch từng ngày: lịch được tính lại mỗi lần mở bằng hàm thuần `packages/core-examplan/examPlan.ts` từ trạng thái học thật, nên người học đi lệch kế hoạch cũng không sinh ra dữ liệu lệch phải đồng bộ.                                                                                                                                                                                                                                                                                                                                              |
| `0071_programming_specializations.sql` | Tiến độ **hướng chuyên sâu** môn Lập trình (nối tiếp PR #712): bảng `programming.spec_enrollment` (hướng đang theo — `primary` là hướng sản phẩm chính, `cross` là hướng nền cắt ngang học song song; partial unique index bắt **một hướng chính mỗi người**) + `programming.spec_stage_progress` (tiến độ chặng S1–S4, khoá `(user_id, stage_id)` theo đúng khuôn `programming.lesson_progress`). Id hướng/chặng lấy từ `packages/subject-programming/specializations/`, tầng API đối chiếu trước khi ghi.                                                                                                                                                                                                                                                                                         |
| `0072_habit_logs_idempotent.sql`       | Chặn check-in thói quen bị tính nhiều lần trong CÙNG MỘT NGÀY. Dồn các bản ghi trùng `(habit_id, logged_at)` đã lỡ tạo thành một (giữ bản cũ nhất, **cộng dồn `count`** nên không mất thông tin) rồi thêm **chỉ mục duy nhất** trên cặp đó. Cần vì `logHabit()` cũ luôn `current_streak + 1` mỗi lần gọi — bấm 5 lần là streak nhảy 5 ngày. Ràng buộc đặt ở tầng CSDL nên hai tiến trình PM2 song song cũng chỉ một cái ghi được. Lũy đẳng (`if not exists`).                                                                                                                                                                                                                                                                                                                                       |
| `0073_programming_path_progress.sql`   | Tiến độ **lộ trình mục tiêu** môn Lập trình (nối tiếp PR #766, đặc tả `2026-08-31-khoa-hoc-ky-su-truong-ai.md` đợt 2): bảng `programming.path_progress`, khoá `(user_id, path_id, stage_id)`, trạng thái `skipped`/`in_progress`/`completed` **chỉ tốt lên** (giữ hạng cao hơn khi ghi đè). Tách khỏi `programming.spec_stage_progress` (0071) vì một chặng có thể được LỘ TRÌNH đánh dấu `skipped` qua chẩn đoán mà người học chưa từng mở trang hướng tương ứng. Id lộ trình/chặng đối chiếu `packages/subject-programming/learningPaths/` trước khi ghi.                                                                                                                                                                                                                                         |
| `0074_programming_path_artifacts.sql`  | Kho **artifact cá nhân** của lộ trình mục tiêu (đặc tả đợt 3, nối tiếp `0073`): bảng `programming.path_artifacts`, khoá `id` (uuid) — KHÔNG khoá theo `(user_id, path_id, phase_id)` vì đây là NHẬT KÝ nộp bài (nộp lại nhiều lần cho cùng giai đoạn tạo dòng mới, không ghi đè), khác `path_progress` vốn là trạng thái hiện tại. Người học tự khai link + ghi chú, KHÔNG chấm bằng AI. Chỉ xoá được artifact của chính mình.                                                                                                                                                                                                                                                                                                                                                                      |
| `0075_daily_plan_completions.sql`      | Bằng chứng **completion do server xác nhận** cho Daily Learning Plan (P1.3a, đặc tả `2026-09-08-p1-3-daily-plan-completion.md`): bảng append-only `public.daily_plan_completions` chỉ ghi khi merge tiến độ chứng minh thẻ từ vựng ĐẾN HẠN thật sự được ôn tiến lên — client không tự khai completion. Unique index theo `(user_id, action_kind, planner_version, ngày Việt Nam)` nên client retry hay đồng bộ nhiều thiết bị cũng chỉ tính một lần. Ràng buộc `check` ép `evidence` đúng một khoá `reviewedCardCount` > 0, **không lưu card id hay nội dung học**.                                                                                                                                                                                                                                 |
| `0076_remove_pro_plus_plans.sql`       | **GĐ1 — xoá gói Pro/Plus** (đặc tả `docs/specs/2026-09-12-gd1-xoa-goi-pro.md`): hệ thống chỉ còn `free` + `vip`. Di trú dữ liệu: người đang trả Plus/Pro **còn hạn** → `vip` **giữ nguyên `plan_expires_at`** (không ai đang trả tiền bị mất quyền lợi đã mua), Plus/Pro **đã hết hạn** → `free` (dọn giá trị chết, `resolvePlan()` vốn đã coi là free). Đặt sàn `app_settings.pro_daily_limit` = 30 — **cột giữ tên cũ nhưng ý nghĩa mới là "hạn mức người dùng MIỄN PHÍ"** (30 = đúng hạn mức Plus cũ), cố ý không đổi tên cột để khỏi phải sửa đồng bộ API + panel admin. **KHÔNG xoá** dữ liệu lịch sử (`payments.plan='pro'`, dòng `plan_prices`/`plan_marketing_*`/`plan_feature_flags` của gói cũ) — tầng ứng dụng đã ngừng đọc chúng. Lũy đẳng; rollback cần CSV export ghi sẵn trong file. |
