# Đặc tả `devops-s1` và Runtime Lab Linux

- Chốt lát cắt `M2/S1e` thành 4 unit / 8 bài Python mô phỏng có ranh giới rõ với Linux thật.
- Khóa các invariant: triage theo evidence, network ladder theo tầng, automation fail-fast/lũy đẳng,
  secret redaction, và backup chỉ hợp lệ sau restore evidence.
- Giữ Runtime Lab (HTTP worker pool, profiler, load test, báo cáo memory/I/O) là artifact Linux
  thật, không thay bằng sandbox.
