# 0325 — 2026-09-15 — Đặc tả slice 02 Góc học tập: Tiếng Anh là một môn ngang hàng

**PR:** (điền khi tạo) · **Loại:** docs-only, không source/schema/dependency ·
**Đặc tả:** `docs/specs/2026-09-15-goc-hoc-tap-02-tieng-anh-la-mot-mon.md` — trạng thái **In review**,
chờ chủ dự án chốt 3 quyết định (§7) trước khi Approved.

## Đã làm

- Khảo sát mã thật trên `main` `9193164` (sau #927): mọi điểm vào Tiếng Anh cấp nền tảng
  (`STUDIOS`, `DesktopSidebar`, `Layout` dropdown, `breadcrumb`, `Subjects` CTA, `Home`,
  `Practice`, `ExamPlan`, hub), route/alias hiện có, cách lưu token/tiến độ theo origin, test và
  E2E đang canh, `codemap impact` (`studios.ts` → 81 file; `EnglishHome.tsx` → 2 file).
- Viết đặc tả theo khuôn `docs/templates/dac-ta-tinh-nang.md`: tiêu chí chấp nhận (11 AC, mỗi AC
  chỉ đúng test/E2E chứng minh) viết TRƯỚC giải pháp; phạm vi LÀM / KHÔNG LÀM tách bạch với slice
  03 (công cụ) và 04 (bỏ mặc định English); bảng điểm chạm từng file; hợp đồng ownership mở rộng
  từ spec cha; helper dùng chung `packages/core-learner/subjectHome.ts` cho cả server lẫn client;
  bất biến + test canh; kế hoạch thi hành 7 bước trong một PR; rollback.

## Phát hiện quan trọng trong lúc khảo sát (lỗi THẬT trên production, ghi ở spec §2.3)

Host mode server đang bật. Trên host `hoc-tap.`, danh mục gọi `nav('/hoc-tieng-anh')` /
`nav('/lap-trinh')` — điều hướng trong app, KHÔNG đổi origin — nên Tiếng Anh/Lập trình chạy trên
origin `hoc-tap.`, nơi `localStorage` (token `gsa_session_token_v1`, tiến độ theo uid) trống:
**người đã đăng nhập bỗng thành khách, tiến độ 0**, và những gì học ở đó ghi vào origin không ai
đọc lại. Luật 301 của server chỉ bắt lượt tải trang, không bắt `navigate()`. Slice 02 đặt AC-6
để bịt bằng một helper biết ownership; không migrate dữ liệu đã lỡ ghi (Q3 chờ chốt).

## Quyết định chờ chủ dự án (spec §7)

| #   | Câu hỏi                                                     | Đề xuất mặc định                                                    |
| --- | ----------------------------------------------------------- | ------------------------------------------------------------------- |
| Q1  | 5 mục con "Học Tiếng Anh" ở sidebar đi đâu sau khi bỏ nhóm? | Không hiện ở sidebar trong 02; đủ 5 nút ở trang tổng quan; 03 quyết |
| Q2  | Nhãn nút danh mục                                           | "Vào môn Tiếng Anh" và "Vào môn Lập trình"                          |
| Q3  | Dữ liệu đã lỡ ghi ở origin `hoc-tap.`                       | Không migrate; ghi nợ có đo được                                    |

## Validation (docs-only)

- `npx prettier --check` file spec + changelog + `PROGRESS.md`: PASS.
- `npx vitest run scripts/changelog.test.ts`: PASS (khuôn tên file, không chồng mục vào PROGRESS).
- Không chạy runtime suite cho docs-only; mọi tên file/hàm/test nêu trong spec đã kiểm bằng
  `grep`/`sed` trên checkout thật (ví dụ: hub không có alias Vite `@dhcb/*`; `underPrefix` chưa
  export; E2E khách nằm ở `login-redirect.spec.ts`/`smoke.spec.ts`).
