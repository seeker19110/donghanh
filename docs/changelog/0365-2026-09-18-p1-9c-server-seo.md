# 0365 — 2026-09-18 — P1-9c server routing và SEO URL Góc học tập

## Phạm vi

- Bổ sung test server cho các đường sâu của `/goc-hoc-tap/programming/**` và
  `/goc-hoc-tap/english/**`, xác nhận chúng thuộc app host và không bị gộp về trang tổng quan.
- Cập nhật sitemap tĩnh sang URL canonical mới của Tiếng Anh.
- Bổ sung hướng dẫn Nginx 301 cho URL Lập trình và các URL Tiếng Anh cũ, kèm lệnh kiểm tra.
- Ghi rõ hai bước vận hành còn cần làm trên VPS: áp dụng config Nginx và đo Google Search Console
  sau 14 ngày.

## Bằng chứng

- `npx vitest run apps/server/src/subjectsRouting.test.ts` — **93/93 pass**.
- Sitemap XML parse hợp lệ bằng PowerShell `[xml]`.
- `npx prettier --write` cho các file Markdown/TypeScript thay đổi và `git diff --check` — pass.

## Rủi ro / việc tay

- Phiên AI không sửa hoặc reload Nginx production. Người vận hành phải áp dụng các `location`
  trong `docs/deploy-vps-ubuntu.md`, chạy `nginx -t`, reload rồi kiểm tra `curl -I`.
- Cần kiểm tra Google Search Console vào **2026-10-01** để xác nhận redirect và canonical mới.
