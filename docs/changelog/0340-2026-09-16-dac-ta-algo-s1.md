# 0340 — Đặc tả bài học thật cho `algo-s1`

**Ngày:** 2026-09-16  
**Phạm vi:** `GOAL-2026-ASA/M2-S1c` — nền tảng thuật toán và độ phức tạp.

## Đã làm

- Chốt bốn unit `p6-u142..p6-u145` và tám bài Python thuần phủ bốn module của chặng.
- Định nghĩa differential test có seed, negative control và bộ đếm phép tính ổn định trong CI.
- Ghi acceptance criteria, test canh, rollout và rollback trước khi viết source.

## Kiểm chứng

- Prettier và `git diff --check` xanh.
- Không sửa runtime; source chỉ được triển khai sau khi đặc tả merge.
