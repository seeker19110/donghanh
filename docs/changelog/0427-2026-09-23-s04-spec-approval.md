# Tích hợp review S04/S05 và duyệt contract S04

- Ngày: 2026-09-23. PR đợt này: chờ cấp số; review nguồn #1128 đã merge.
- Base: `bec484dff7129145c531cacbb321d2942ed07b7a`; docs-only, không sửa source.
- S04: Codex duyệt theo ủy quyền chủ sản phẩm; chốt phiên/Retry, snapshot dữ liệu,
  nhánh phát âm chi tiết, lỗi callback/API, feedback; đủ metadata, AC/proof và DESIGN_SPEC.
- S05: tích hợp span/distractor/thống kê/guard/manifest, giữ Draft. Chờ S04 merge với
  evidence và Product chốt người duyệt chuyên môn/cách nghiệm thu nếu B thiếu 20 mẫu thật.
- Kiểm trên Node 22.23.2 sau `npm ci --ignore-scripts`: Prettier check bốn file đạt;
  `check:protocol -- --ci` không vi phạm; `check:specs -- --ci` kiểm 139 spec không
  thiếu path; `vitest run scripts/changelog.test.ts` đạt 8/8; progress freshness không
  cảnh báo; `git diff --check` đạt. Không chạy proof sản phẩm, không claim test/ảnh/pilot.
- Đồng bộ PROGRESS và goal; không cần đổi PROJECT/CLAUDE/ADR/ops/TRAPS vì không đổi
  kiến trúc, vận hành hoặc phát hiện sự cố mới. Rollback: revert tài liệu, không migration.
