# 0381 — 2026-09-19 — ADR-0008: chốt 3 câu hỏi, chuyển Accepted, giao B1+B2

## Tóm tắt

`docs/adr/0008-cham-lai-server-lap-trinh-ngoai-p1-p4.md` chuyển từ "đề xuất" sang **Accepted**
theo chỉ đạo trực tiếp của chủ dự án ("chốt luôn 3 câu hỏi rồi giao subagent theo hướng tốt").
Ba câu hỏi mở trong ADR được chốt:

1. **JS/TS + html/dom/fetch (97 bài, B3):** chấp nhận `node:vm` tối giản (context rỗng, không
   expose `require`/`process`/`global`, timeout cứng — giống `lessonsJs.test.ts` hiện tại) —
   VỚI ĐIỀU KIỆN BẮT BUỘC: `html`/`dom`/`fetch` phải sửa từ `new Function(...)` trần sang chạy
   trong `vm.createContext` trước khi được coi là an toàn tương đương JS/TS. B3 là ADR/PR riêng
   sau, không chặn B1+B2.
2. **Bước dự án (`p<n>-s<x>`):** giữ ngoài phạm vi (mặc định KHÔNG mở rộng).
3. **SQL (5 bài):** để ngoài B1/B2, xác minh cấu hình `sqlWorker.ts` ở PR riêng nhỏ sau.

Đã giao subagent thi hành B1+B2 (374 bài: Python P5/P6/7 khoá ngắn + Kotlin/bash/4 khoá mô
phỏng git-hermes-vibe-openclaw) ngay trong đợt việc kế tiếp (PR riêng).

## Issue / outcome

ADR-0008 trước đó ở trạng thái "đề xuất, chờ chủ dự án" — chặn B1+B2 (374 bài an toàn, chi phí
thấp) khỏi thi hành dù không có rủi ro bảo mật thật. Chốt câu hỏi ngay giải phóng phần việc rẻ
mà không phải chờ quyết định khó (B3) xong.

## Research / spec

`docs/adr/0008-cham-lai-server-lap-trinh-ngoai-p1-p4.md` (nay Accepted).

## Validation

- Đọc lại toàn bộ ADR + bằng chứng đã có trước khi chốt (không quyết định mù) — không phát sinh
  thay đổi số liệu, chỉ chốt hướng đi cho 3 câu hỏi đã đặt sẵn.
- `PROGRESS.md` cập nhật TẠI CHỖ dòng nợ liên quan, không chồng mục mới.

## Rủi ro, rollout và rollback

- **Rủi ro:** tài liệu quyết định, không đổi hành vi hệ thống trong PR này.
- **Rollout:** không áp dụng — B1+B2/B3/SQL thi hành ở các PR riêng theo sau.
- **Rollback:** revert PR nếu chủ dự án muốn viết lại hướng quyết định.

## Definition of Done

- [x] 3 câu hỏi trong ADR-0008 đã chốt, có lý do rõ ràng cho từng lựa chọn.
- [x] Trạng thái ADR chuyển Accepted.
- [x] `PROGRESS.md` phản ánh đúng: B1+B2 đang triển khai, B3+SQL vẫn mở với hướng đã chốt.
- [x] Giao việc B1+B2 cho subagent theo brief đầy đủ ngữ cảnh (đường dẫn file, quyết định, tiêu
      chí chấp nhận).
