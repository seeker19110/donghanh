# S07 — Khoảng trống bằng chứng zoom, focus và vùng chạm

- Đối chiếu `origin/main` tại `422c9134` ngày 2026-09-23. Đây là audit tài liệu, không phải chứng nhận accessibility hay báo cáo lỗi UI đã tái hiện.
- [Đặc tả S06–S08](../specs/2026-09-23-uiux-s06-s08-accessibility.md) chỉ ghi **S08 Approved for implementation**; S07 chưa có quyết định Approved riêng. PR #1126 đã merge S07a (cho phép zoom) tại `6f8054ff`, không phê duyệt toàn bộ phần S07. Vì vậy lượt này không sửa source.

## Bằng chứng đã có và giới hạn chính xác

| Bằng chứng ở main                                                                                                        | Chứng minh được                                                                                                                                                                                                                         | Chưa chứng minh được                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`e2e/zoom-permission.spec.ts`](../../e2e/zoom-permission.spec.ts)                                                       | Meta viewport không khóa zoom, axe `meta-viewport` không có vi phạm/incomplete, `touch-action` ở html/body cho phép pinch; `/welcome` không tràn ngang tại 320/390/768/1440 trong ba theme. Negative control khóa zoom làm axe báo lỗi. | Gesture pinch trên máy thật, browser zoom 200%, route học/quiz/modal, nội dung ở trạng thái tải/lỗi, focus và hit target. `setViewportSize` không thay browser zoom. |
| [`e2e/modal-sticky-header.spec.ts`](../../e2e/modal-sticky-header.spec.ts)                                               | Header Modal không chồng nội dung đầu ở 320/390/1440; sheet mục lục tại 390 vẫn dính khi cuộn.                                                                                                                                          | Hình học sau browser zoom 200%, phần tử đang focus có bị header che, bàn phím ảo.                                                                                    |
| [`e2e/a11y-modals.spec.ts`](../../e2e/a11y-modals.spec.ts), [`e2e/outline-stem.spec.ts`](../../e2e/outline-stem.spec.ts) | Axe A/AA cho sheet mục lục ở 390 theo theme, Escape đóng; ca STEM chọn bài đưa focus tới tiêu đề.                                                                                                                                       | Vòng Tab/Shift+Tab và focus sau Escape tại browser zoom 200%, target 44×44 đo từ bounding box thực tế. Không nên gọi đây là lỗi focus đã xác nhận.                   |
| [`e2e/mobile-layout-guards.spec.ts`](../../e2e/mobile-layout-guards.spec.ts)                                             | Đo lề dưới và tràn ngang cho một tập route ở 320/390 khi không zoom.                                                                                                                                                                    | 200% browser zoom, tất cả màn S07 và bàn phím ảo.                                                                                                                    |

[#1126](https://github.com/seeker19110/donghanh/pull/1126) có `quality` và `e2e` thành công trên head cuối; một metadata failure cũ đã được thay bằng metadata success trước merge. [Bằng chứng S07a](2026-09-23-s07a-zoom-evidence.md) chỉ ghi 4/4 Playwright mô phỏng viewport và ảnh `/welcome` 390/1440. Không chuyển số này thành kết quả ma trận S07.

## Ca ưu tiên để kiểm sau khi duyệt source S07

**P1: sheet “Mục lục môn học” khi browser zoom 200%.** Đây là ca giao nhau giữa danh sách bài dài, sticky header, focus trap và nút đóng cố định; ở 390 CSS px, chiều cao hữu dụng giảm thêm nếu có bàn phím ảo. URL fixture: `/goc-hoc-tap/physics/bai-hoc/ly10-c2-b10--su-roi-tu-do`, mở nút “Mục lục môn học”. Kiểm trên ba theme: mở bằng bàn phím, Tab/Shift+Tab qua liên kết đầu/cuối, Escape trả focus về trigger, mở lại rồi chọn bài để focus tới tiêu đề mới. Với mỗi bước, đo `getBoundingClientRect()` của phần tử focus so với mép header và viewport, `elementFromPoint()` tại tâm các nút/liên kết, và bounding box ≥44×44 CSS px cho vùng tương tác thuộc phạm vi sửa. Ghi scrollWidth−clientWidth ≤1px ở vùng văn xuôi; vùng cây cuộn riêng phải còn đọc/chọn được. Nếu một phép đo thất bại, lưu route, theme, kích thước cửa sổ, mức zoom, DOM target và ảnh/trace trước khi chọn sửa cục bộ.

Playwright viewport 320px chỉ giúp tái hiện áp lực reflow trong CI; nó không phải bằng chứng browser zoom 200%. Ca browser zoom cần ghi phiên bản browser/OS, mức zoom thực, thao tác bàn phím và ảnh trước/sau. Pinch và bàn phím ảo cần thiết bị thật, ghi model/OS/browser. Không có dữ liệu thiết bị thật trong lượt audit này.

## Điều kiện mở source và kết luận

1. Review phạm vi S07 trên main, ghi quyết định **Approved for implementation** vào spec và merge PR tài liệu trước source; giữ S07a và S07 rộng là hai trạng thái khác nhau.
2. Chạy ca ưu tiên và xác nhận lỗi cụ thể bằng trace/đo hình học. Nếu có lỗi, chọn đúng một thành phần/route để sửa, chạy `codemap -- impact`, thêm E2E đo hành vi và negative control phù hợp; kiểm ảnh 390/1440 trước/sau và các gate Node 22.
3. Ma trận nghiệm thu còn mở: Home, placement/onboarding, STEM + kết quả, CEFR quiz, outline modal, input focus × 320/390/768/1440 × ba theme, trạng thái có thật; browser zoom 200%, keyboard/focus/44px, pinch và bàn phím ảo trên thiết bị thật. Ghi N/A có lý do thay vì coi là đạt.

**Trạng thái S07: PARTIAL / WAITING.** Cấu hình zoom đã sửa và CI S07a đạt; chưa có bằng chứng để kết luận toàn bộ zoom/reflow/focus/44px hay ba theme trên thiết bị thật.
