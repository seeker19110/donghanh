# S07 — probe zoom thật trên ba màn học còn lại

**Trạng thái: PARTIAL / cần kiểm tiếp.** Probe này bổ sung số đo cho [khoảng trống S07](2026-09-23-s07-followup-evidence-gap.md), không duyệt source S07 hoặc chứng nhận WCAG. Source được đo tại `9c3e2fa27d9da5b4ea5675f7abaec4abe3af2106` (23/09/2026); các commit sau đến thời điểm ghi báo cáo chỉ đổi tài liệu. Dùng Chrome 153.0.8010.53 headless trên Windows, tài khoản và API giả lập, không gọi dịch vụ thật. Bộ chạy và `results.json` ở ngoài repo: `C:\Users\liend\.codex\uiux-implementation\qa-s07-remaining\`; SHA256 của JSON `010aa08c9c7317ce20a6eb8e22d92add245e9cdab636cfce9816ccf8386c435b`.

Trong cùng profile Chrome, đặt Page zoom từ 100% sang 200% qua `chrome://settings/appearance`. Cửa sổ 780×844 cho `devicePixelRatio` 1→2 và `innerWidth` 780→390 CSS px; cửa sổ 2880×1800 cho `innerWidth` 1440 CSS px. `visualViewport.scale` vẫn 1; đây là page zoom thật, không phải pinch. Mỗi route được chạy trong ba theme `dark-blue`, `blue-sky`, `kid` và hai kích thước CSS 390/1440.

| Route và trạng thái                                                   | Kết quả đo                                                                                         | Giới hạn                                                                                             |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Trang chủ `/`                                                         | 6/6 ca nạp được, `document` và `body` không tràn ngang, DPR 2.                                     | API tổng quan giả lập trả 503; đây là trạng thái fallback, không chứng minh Home với dữ liệu thật.   |
| Placement `/placement`                                                | 6/6 ca nạp được, không tràn ngang, DPR 2.                                                          | Chỉ màn bắt đầu, chưa đi hết câu hỏi/kết quả/lưu hồ sơ.                                              |
| Kết quả STEM `/goc-hoc-tap/physics/bai-hoc/ly10-c2-b10--su-roi-tu-do` | 6/6 ca nộp với response server giả lập, vùng kết quả hiện, không tràn ngang, DPR 2.                | Chỉ một bài Vật lí và một response cố định; chưa chứng minh kết quả thật, owner switch hoặc bốn môn. |
| CEFR quiz `/goc-hoc-tap/english/lo-trinh/a1?tab=quiz`                 | Lượt đầu 5/6 ca timeout do harness chờ `h1`; chạy lại với `h2` đạt 6/6 ca nạp và không tràn ngang. | Đây là probe hình học một câu hỏi, không chứng minh toàn bộ quiz hoặc nghiệm thu accessibility.      |

Trên 18 ca của ba route đầu, phép đo `scrollWidth − clientWidth` của document/body đều bằng 0. Bộ đo đếm phần tử tương tác có bounding box dưới 44×44 CSS px, tối thiểu 2 và tối đa 24 tùy màn/kích thước. Con số thô **không phải số vi phạm WCAG**: nó gồm skip link 1×1 khi không focus và một số control có khoảng cách với lân cận. Các mục cần review cụ thể ở 390 CSS px: ô nhập câu hỏi Home cao 24 px; nút “Lộ trình CEFR” và “Bỏ qua — tự chọn trình độ” trên Placement cao 36 px; nút “Góc học tập” trên STEM cao 36 px. Đo lại vùng hit thực, khoảng cách và ngoại lệ trước khi quyết định sửa.

## Chạy lại CEFR và kiểm ảnh bị cắt

Lượt bổ sung dùng cùng source, Chrome và Page zoom 200%, mở thẳng `?tab=quiz` theo contract URL của `CefrLevelPage.tsx` và chờ `h2` của câu hỏi. Cả 6/6 tổ hợp có `devicePixelRatio = 2`, `document` không tràn ngang và nội dung câu hỏi xuất hiện. `results.json` mới ở `C:\Users\liend\.codex\uiux-implementation\qa-s07-cefr-rerun\` có SHA256 `e7e3407f4e2dcaf5023768af5c7748f15caac62d2deeedc5dc34b6577b31e409`. Lỗi timeout lần đầu là lỗi locator của harness, không phải lỗi tải quiz.

Ảnh Playwright ở 390 CSS px chỉ cho thấy nửa trái của hàng tab do cách chụp khi Chrome page zoom 200%; không dùng ảnh này để kết luận tab bị cắt. Probe DOM độc lập trong `qa-s07-cefr-geometry` đo cả sáu nút nằm từ x=16 đến x=374 CSS px trong viewport 390 px, parent `scrollWidth = clientWidth = 358`, và `elementFromPoint` tại tâm từng nút trả về đúng nút/con của nó. Không có lỗi reflow hàng tab được tái hiện. Hai control có box dưới 44×44 ở CEFR 390 là skip link 1×1 khi chưa focus và nút “Tiếng Anh” 36×36; cần review hit target/khoảng cách cho nút sau.

Tab Quiz hiện render `h2` câu hỏi nhưng không render `h1` của cấp (nhánh `activeTab !== 'lessons'` dùng `p`), và `document.title` trong probe vẫn là tiêu đề ứng dụng chung. Đây là phát hiện ngữ nghĩa cần review riêng với trình đọc màn hình, không phải lỗi zoom đã chứng minh. Không sửa source trong PR bằng chứng này khi S07 rộng chưa Approved.

Probe riêng cho focus của kết quả STEM (`qa-s07-stem-focus/results.json`, SHA256 `e5fd943a8c5edf83082211c621afb53ce709aa826da77315d8533282abf800a7`) xác nhận “Làm lại” và “Bài tiếp theo” đều khớp `:focus-visible`, có ring cyan qua `box-shadow` rộng 4 px. `outline-style: none` ở chúng là do `buttonClass()` dùng ring thay outline, không phải bằng chứng focus vô hình. Hai liên kết bài trước/sau dùng outline solid 2 px. Không mở lỗi focus mới từ phép đo outline đơn lẻ.

Probe chưa đo 320/768 CSS px, pinching và bàn phím ảo thiết bị thật, browser khác, vòng focus trap, tương phản chữ thật hoặc screen reader. Ảnh chụp chưa đáng tin tại page zoom trong harness này; dùng số đo DOM và ảnh chụp bằng phương pháp khác trước kết luận thị giác. S07 tiếp tục **PARTIAL**; hướng tiếp theo là kiểm các control ngắn, heading Quiz bằng AT và thiết bị thật trước nghiệm thu.
